type Query {
  user(id: ID!): User
}

type Mutation {
  createUser(email: String!, password: String!, isAdmin: Boolean!): User
}

type User {
  id: ID!
  email: String!
  password: String!
  isAdmin: Boolean!
}
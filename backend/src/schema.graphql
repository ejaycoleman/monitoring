type Query {
  currentUser: User!
  tasks: [Task]
}

type Mutation {
  register(email: String!, password: String!, isAdmin: Boolean!): User
  login(email: String!, password: String!): LoginResponse
  uploadTasksFile(tasks: String!): Boolean
}

type User {
  id: ID!
  email: String!
  isAdmin: Boolean!
}

type LoginResponse {
  token: String
  user: User
}

type Task {
  number: Int!,
  command: String!,
  frequency: Int!,
  period: String!
}

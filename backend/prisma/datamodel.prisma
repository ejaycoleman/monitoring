type User {
  id: ID! @id
  isAdmin: Boolean! @default(value: false)
  email: String! @unique
  password: String!
}

type Task {
  id: ID! @id,
  command: String!,
  frequency: Int!,
  period: String!
}

type Executions {
  id: ID! @id,
  task: ID!,
  datetime: DateTime!
}
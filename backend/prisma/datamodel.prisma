type User {
  id: ID! @id
  isAdmin: Boolean! @default(value: false)
  email: String! @unique
  password: String!
}

type Task {
  id: ID! @id,
  number: Int! @unique,
  command: String!,
  frequency: Int!,
  period: String!,
  executions: [Execution!]
}

type Execution {
  id: ID! @id,
  task: Task,
  datetime: Int!
}
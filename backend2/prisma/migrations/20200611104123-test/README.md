# Migration `20200611104123-test`

This migration has been generated by Elliott Coleman at 6/11/2020, 10:41:23 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."Task" (
"approved" BOOLEAN NOT NULL  ,"authorId" INTEGER NOT NULL  ,"command" TEXT NOT NULL  ,"frequency" INTEGER NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"number" INTEGER NOT NULL  ,"period" TEXT NOT NULL  ,FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."Execution" (
"datetime" INTEGER NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"taskId" INTEGER NOT NULL  ,FOREIGN KEY ("taskId") REFERENCES "Task"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."TaskNotification" (
"taskId" INTEGER NOT NULL  ,"userId" INTEGER NOT NULL  ,
    PRIMARY KEY ("userId","taskId"),FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("taskId") REFERENCES "Task"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."Preference" (
"executionThresholdAbsolute" TEXT NOT NULL  ,"executionThresholdIdeal" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT)

CREATE TABLE "quaint"."new_User" (
"email" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"isAdmin" BOOLEAN NOT NULL DEFAULT false ,"password" TEXT NOT NULL  ,"preferenceId" INTEGER NOT NULL  ,FOREIGN KEY ("preferenceId") REFERENCES "Preference"("id") ON DELETE CASCADE ON UPDATE CASCADE)

INSERT INTO "quaint"."new_User" ("email", "id") SELECT "email", "id" FROM "quaint"."User"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."User";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "quaint"."User_preferenceId" ON "User"("preferenceId")

CREATE UNIQUE INDEX "quaint"."Task.number" ON "Task"("number")

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Post";;
PRAGMA foreign_keys=on

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200611104123-test
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,55 @@
+datasource db {
+  provider = "sqlite"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id    Int     @id @default(autoincrement())
+  isAdmin Boolean @default(false)
+  email String  @unique
+  password String
+  tasks Task[]
+  notifications TaskNotification[]
+  preferenceId Int
+  preference Preference @relation(fields: [preferenceId], references: [id])
+}
+
+model Task {
+  id    Int     @id @default(autoincrement())
+  authorId Int
+  author User @relation(fields: [authorId], references: [id])
+  approved Boolean
+  number Int @unique
+  command String
+  frequency Int
+  period String
+  executions Execution[]
+  notifications TaskNotification[]
+}
+
+model Execution {
+  id Int @id @default(autoincrement())
+  taskId Int
+  task Task @relation(fields: [taskId], references: [id])
+  datetime Int
+}
+
+model TaskNotification {
+  // id Int @id @default(autoincrement())
+  userId Int
+  user User @relation(fields: [userId], references: [id])
+  taskId Int
+  task Task @relation(fields: [taskId], references: [id])
+  @@id([userId, taskId])
+}
+
+model Preference {
+  id Int @id @default(autoincrement())
+  forUser User
+  executionThresholdIdeal String
+  executionThresholdAbsolute String
+}
```



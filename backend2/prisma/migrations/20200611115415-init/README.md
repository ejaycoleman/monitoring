# Migration `20200611115415-init`

This migration has been generated by Elliott Coleman at 6/11/2020, 11:54:15 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."User" (
"email" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"isAdmin" BOOLEAN NOT NULL DEFAULT false ,"password" TEXT NOT NULL  )

CREATE TABLE "quaint"."Task" (
"approved" BOOLEAN NOT NULL  ,"authorId" INTEGER NOT NULL  ,"command" TEXT NOT NULL  ,"frequency" INTEGER NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"number" INTEGER NOT NULL  ,"period" TEXT NOT NULL  ,FOREIGN KEY ("authorId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."Execution" (
"datetime" INTEGER NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"taskId" INTEGER NOT NULL  ,FOREIGN KEY ("taskId") REFERENCES "Task"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."TaskNotification" (
"taskId" INTEGER NOT NULL  ,"userId" INTEGER NOT NULL  ,
    PRIMARY KEY ("userId","taskId"),FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("taskId") REFERENCES "Task"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."Preference" (
"executionThresholdAbsolute" TEXT NOT NULL  ,"executionThresholdIdeal" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"userId" INTEGER NOT NULL  ,FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "quaint"."Task.number" ON "Task"("number")

CREATE UNIQUE INDEX "quaint"."Preference_userId" ON "Preference"("userId")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200611104123-test..20200611115415-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -13,10 +13,9 @@
   email String  @unique
   password String
   tasks Task[]
   notifications TaskNotification[]
-  preferenceId Int
-  preference Preference @relation(fields: [preferenceId], references: [id])
+  preference Preference
 }
 model Task {
   id    Int     @id @default(autoincrement())
@@ -38,9 +37,8 @@
   datetime Int
 }
 model TaskNotification {
-  // id Int @id @default(autoincrement())
   userId Int
   user User @relation(fields: [userId], references: [id])
   taskId Int
   task Task @relation(fields: [taskId], references: [id])
@@ -48,8 +46,9 @@
 }
 model Preference {
   id Int @id @default(autoincrement())
-  forUser User
+  userId  Int
+  forUser User @relation(fields: [userId], references: [id])
   executionThresholdIdeal String
   executionThresholdAbsolute String
 }
```



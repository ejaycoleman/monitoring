# Migration `20200611162522-idk`

This migration has been generated by Elliott Coleman at 6/11/2020, 4:25:22 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"email" text  NOT NULL ,"id" SERIAL,"isAdmin" boolean  NOT NULL DEFAULT false,"password" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Task" (
"approved" boolean  NOT NULL ,"authorId" integer  NOT NULL ,"command" text  NOT NULL ,"frequency" integer  NOT NULL ,"id" SERIAL,"number" integer  NOT NULL ,"period" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Execution" (
"datetime" integer  NOT NULL ,"id" SERIAL,"taskId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."TaskNotification" (
"taskId" integer  NOT NULL ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("userId","taskId"))

CREATE TABLE "public"."Preference" (
"executionThresholdAbsolute" text  NOT NULL ,"executionThresholdIdeal" text  NOT NULL ,"id" SERIAL,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "Task.number" ON "public"."Task"("number")

CREATE UNIQUE INDEX "Preference_userId" ON "public"."Preference"("userId")

ALTER TABLE "public"."Task" ADD FOREIGN KEY ("authorId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Execution" ADD FOREIGN KEY ("taskId")REFERENCES "public"."Task"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."TaskNotification" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."TaskNotification" ADD FOREIGN KEY ("taskId")REFERENCES "public"."Task"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Preference" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200611160532-init..20200611162522-idk
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
-  provider = "postgres"
-  url = "***"
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
```


